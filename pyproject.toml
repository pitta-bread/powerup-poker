[project]
name = "powerup-poker"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "pbeards", email = "peterbeardsmore@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
powerup-poker = "powerup_poker:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
